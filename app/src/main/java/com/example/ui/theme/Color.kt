package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Palette (Based on Tailwind OKLCH light values)
val LightBg = Color(0xFFFFFFFF)            // Pure white background (oklch 1.0000 0 0)
val LightCardBg = Color(0xFFFFFFFF)        // Card background (oklch 1.0000 0 0)
val LightPrimary = Color(0xFFD97706)       // Honey gold/amber (oklch 0.7686 0.1647 70.0804)
val LightSecondary = Color(0xFF64748B)     // Cool steel gray
val LightBorder = Color(0xFFE2E8F0)        // Clean dividing border

// Dark Palette (Based on Tailwind OKLCH dark values)
val DarkBg = Color(0xFF121214)             // Pitch charcoal canvas (oklch 0.2046 0 0)
val DarkCardBg = Color(0xFF1C1C1E)         // Smooth elevated dark surface (oklch 0.2686 0 0)
val DarkPrimary = Color(0xFFF59E0B)        // Brilliant vibrant light gold/amber
val DarkSecondary = Color(0xFF27272A)      // Dark slate secondary accent
val DarkBorder = Color(0xFF37373A)         // Elegant zinc/charcoal container border

// Accents (Clean alert badges)
val SuccessGreen = Color(0xFF10B981)       // Dynamic active indicator
val WarningAmber = Color(0xFFF59E0B)       // Attention color
val AlertRose = Color(0xFFEF4444)          // Alert / Error / Danger color
val InfoNavy = Color(0xFF3B82F6)           // Navigation / Info label
